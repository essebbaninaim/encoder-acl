{
  "output" : "./output/encoder/multibert/FacebookAI_roberta-large/relbert/prompt3",
  "encodeur" : {
    "model_name": "multibert",
    "based_encoder": "FacebookAI/roberta-large",
    "batch_size": 256,
    "lr": 1e-5,
    "max_length": 64,
    "temperature": 0.03,
    "dataset": "./output/dataset/others/relbert_dataset.csv",

    "templates": ["People often use the word <mask> to describe the relationship between [HEAD] and [TAIL] ."]
  }
}
